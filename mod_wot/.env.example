# ============================================================================
# Battle Data Collector - Configuration Environnement
# ============================================================================
# Copiez ce fichier en '.env' et remplissez vos valeurs

# API locale (FastAPI) — tous les appels externes passent par elle
INTERNAL_API_BASE_URL=http://127.0.0.1:8000/api

# Header optionnel si l'API est protégée (INTERNAL_API_KEY côté api/.env)
INTERNAL_API_KEY=

# IMPORTANT: la clé Wargaming se configure désormais côté API (api/.env)
# WARGAMING_APP_ID=<votre_application_id>

# Région du serveur
# Options: eu, na, ru, asia
SERVER_REGION=eu

# Activer la collecte de statistiques (true/false)
COLLECT_PLAYER_STATS=true

# Timeout API en secondes
API_TIMEOUT=5

# Dossier de sortie des données (relatif au dossier WoT)
OUTPUT_DIR=battle_data

# Mode debug (true/false)
DEBUG_MODE=false
